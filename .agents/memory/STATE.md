# 🧠 Agent State & Working Memory

## Current Focus
- Resolved Supabase migration integration issues ("Profile not found" and blank data loading).
- Aligned networking and models with Bruno collection (`/home/iqbalf/Talangraga/bruno/Talangraga-Umroh-Supabase`).

## Active Tasks
- [x] Initialized `.agents/AGENTS.md` and reference guides.
- [x] Authored ADR-0002 for Go backend service migration.
- [x] Implemented Material You & M3 semantic color scheme across multiplatform targets.
- [x] Updated ThemePreference & ThemeManager with dynamic color state.
- [x] Modernized ProfileScreen theme settings UI with FilterChips and Material You switch.
- [x] Verified all Multiplatform & Android builds (`allTests` and `:androidApp:assembleDebug` passed).
- [x] Fixed Ktor `HttpClientFactory` `Authorization` header collision with anon key.
- [x] Added `getCurrentUser()` fallback in `ApiService.getLoginProfile()`.
- [x] Verified Supabase live authentication & data loading (profiles, periodes, session).
- [x] Implemented profile image upload to Supabase Storage during user registration (aligned with web implementation).

## Blockers / Notes
- No current blockers. Live Supabase queries and avatar upload verified successfully.

